#include <stdio.h>
#include <stdlib.h>

int showMenu(char *menuTitle, char *menuOptions[], int menuSize, char *inputMsg);

int main()
{
    char menuTitle[]= "Menu", enter[256]
    printf("Enter menu to get a menu:  \n");
    if(scanf("%s", &enter)&&menuTitle==enter)
        showMenu(char *menuTitle, char *menuOptions[], int menuSize, char *inputMsg)
    return 0;
}

int showMenu(char *menuTitle, char *menuOptions[], int menuSize, char *inputMsg)
 {

 }
